# Request 1 processor on 1 node for 1 minute
#
# Request processors and nodes
#
#PBS -l nodes=1:ppn=6
#
# Request Walltime
#
#PBS -l walltime=24:00:00
#
# Request 10 megabyte of memory per process
#PBS -l pmem=10gb
#
# Request regular output and terminal output to go to same file
#
#PBS -j oe

# SCRIPT body

cd $PBS_O_WORKDIR
date
module load openmpi/gnu
module load gcc/5.2.0
# run program with following command line arguments: Gie_mean, BETA, BETA_SUPERSYNAPSE, A_P, A_D, ACTIVATION, 
#                                                   SUPERSYNAPSE_THRESHOLD, G_MAX, N_RA, num_inh_clusters_in_row, num_inh_in_cluster, N_ss, N_TR, outputFolder
mpirun ./out 0.6 0.995 0.995 0.0015 0.00010 0.003 0.01 0.035 100 4 2 2 2 Output5/
date
